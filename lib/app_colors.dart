import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF080909);     // Main background
  static const Color surface = Color(0xFF15171C);        // Cards
  static const Color surface2 = Color(0xFF1D2027);       // Elevated cards

  static const Color primary = Color(0xFFE50914);        // Netflix Red
  static const Color accent = Color(0xFFFFB800);         // Yellow buttons

  static const Color border = Color(0xFF2A2D35);         // Borders
  static const Color divider = Color(0xFF30333B);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF9CA3AF);
}